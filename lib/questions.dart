class Questions {
  String questionText;
  bool questionAnswer;

  Questions({required this.questionText, required this.questionAnswer});
}

Questions newQuestions = Questions(questionText: 'text', questionAnswer: true);